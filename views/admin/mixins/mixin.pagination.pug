mixin pagination(pagigation)  
  //- pagination phân trang    
  //- biến pagination được controller render ra để sử dụng, những cái file nào mà đc cái file pug trong 
  //- phần controller kia include hay extends thì đều có thể sử dụng các biến được render 
  nav(aria-label='Page navigation example')
  ul.pagination
    li.page-item
      if(pagigation.currentPage > 1)
        a(
          class="page-link"
          href=`?page=${pagigation.currentPage-1}`
          aria-label="Previous"
        )
          span(aria-hidden='true') &laquo;

    //- duyệt số trang
    -for(let i=1;i<=pagigation.totalPage;i++)
      li(class=`page-item ${pagigation.currentPage == i ? "active" : ""}`)
        a(class='page-link'
        href=`?page=${i}`) #{i}

    li.page-item
      if(pagigation.currentPage < pagigation.totalPage)
        a(
          class="page-link"
          href=`?page=${pagigation.currentPage+1}`
          aria-label="Next"
        )
          span(aria-hidden='true') &raquo;
